var exec = require('child_process').exec;

var init  = function() {
	/**
	 *	Place holder for future initializing tasks
	 */

	// console.log( ' ' );
	// console.log( '...Initializing s3f on this first run' );
	// console.log( ' ' );
}

module.exports = init;
